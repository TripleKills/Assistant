package org.sa.studyassistant.model;

public class Trace {
	//_id(INTEGER), item_id(INTEGER), time(LONG), action(TEXT), phase(INTEGER)
	public int phase, item_id;
	public long time;
	public String action;
}
